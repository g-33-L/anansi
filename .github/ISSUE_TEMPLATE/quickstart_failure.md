---
name: Quickstart / install failure
about: docker compose up -d, or the SDK/MCP quickstart, didn't work as documented
title: ""
labels: quickstart
assignees: ""
---

## Which step failed

<!-- e.g. "step 2, option A (ollama pull)", "docker compose up -d itself", "npm install anansi-memory", "npx -y anansi-mcp", "pip install anansi-memory" -->

## What you expected

<!-- What the README/quickstart said should happen. -->

## What actually happened

<!-- Full error message, HTTP status code, or terminal output. Check http://localhost:3000/status first if this is a self-hosted instance — paste its output here too. -->

## Environment

- OS: <!-- e.g. macOS 15, Ubuntu 24.04, WSL2 -->
- Docker version: <!-- `docker --version` -->
- Node.js version: <!-- `node --version`, if relevant -->
- Fresh clone, or an existing checkout?

## Additional context

<!-- Anything you tried that didn't fix it, or workarounds you found. -->
